# Multiplayer follow-up — local execution evidence, 2026-09-22

Plan: [followup-reliable-project-multiplayer](../../../plans/followup-reliable-project-multiplayer.md).
Host: Linux; source checkout and a staged Linux native debug app (not macOS).
The first sections preserve the initial F5–F7 checkpoint; the Linux continuation
below supersedes its native-environment blocker. This is a partial
execution checkpoint, **not** a certifying surface run or a production rollout.

## Fixed and regression-tested

| Item | Fail-without observation | With the fix |
| --- | --- | --- |
| F5: refused pulled path | Three assertions failed: the resolver returned a fallback target, hostile-path pull wrote a fallback, and an undeclared-group pull wrote a flat fallback. | Refused present paths return null; those assertions pass. Missing-path legacy pull and valid nested/manifest paths still pass. |
| F6: pull-pin release race | Temporarily removing only `&& bySlug.has(slug)` from the existing release condition detached the agent during a stale scan; the same-agent assertion failed. The original guard was restored. | A real directory listing is captured without the body; the body is restored before the scan resumes. The agent remains attached. A fresh scan subsequently releases the pin, and `syncable:false` detaches it. No net runtime change for F6. |
| F7: render verification | Executing the old workflow shell with the new virtual-clock fixture failed the idle-rollover case and exhausted 90 probes in each failure case. | The actual workflow shell passes six cases: idle rollover, already current, wrong version, unavailable service, malformed JSON, unhealthy service. Failure is bounded to three probes and two sleeps. |

The render fixture replaces only curl/sleep process behavior; bash and jq execute
the workflow's actual shell. Its idle threshold comes from `apps/render/worker.mjs`.
This proves retry/idle scheduling, not Cloudflare's real rollout timing. Real
traffic can still keep an old container alive; the check then fails visibly.

## Verification commands and results

Commands run from the named directory:

- `apps/studio`: `bun test ./test/sync-*.test.ts --timeout 20000`
  — **1,202 pass, 0 fail**, 70 files, 9,089 assertions, 190.28 s.
- `apps/hub`: `node --test --test-force-exit test/*.test.mjs`
  — **987 pass, 0 fail**, 96 suites, 30.59 s. The force-exit option means this
  run is not evidence of clean process/handle shutdown.
- `apps/render`: `bun test ./test` — **9 pass, 0 fail**, 38 assertions.
- `apps/studio`: `bun test ./test/sync-path-pull.test.ts ./test/sync-remote-docs.test.ts ./test/sync-pull-manifest-path.test.ts`
  — final focused re-run **43 pass, 0 fail**, 89 assertions.
- `apps/studio`: `bunx tsc --noEmit --pretty false` — exit 0.
- Root: `pnpm exec biome check apps/studio/sync/remote-docs.ts apps/studio/test/sync-path-pull.test.ts apps/studio/test/sync-remote-docs.test.ts apps/render/test/deploy-verification.test.ts`
  — four files clean, no fixes needed.
- Root: `node cli/bin/maude.mjs doctor --json` — exit 0; no hard missing
  dependencies or schema errors. Optional dependencies/stack-detection warnings
  were not changed as part of this follow-up.

The committed `apps/studio/dist/` artifacts stayed unchanged before/after tests
and smoke. No release build, version bump, deploy, or remote dataset mutation.

## Partial F3 evidence: local T32 crash oracle

Root command: `node scripts/dev/t32-verify.mjs --n 40 --rounds 4` — exit 0,
`ok: true`. The local SQLite-backed hub is killed and restarted each round;
the fixture cleans up its temporary state.

| Round | Acknowledged writes lost | Duplicate revisions | Duplicate transactions | Head is last acknowledged | Retry | In-flight revisions |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 0 | 0 | 0 | true | accepted | 1 |
| 2 | 0 | 0 | 0 | true | accepted | 1 |
| 3 | 0 | 0 | 0 | true | accepted | 1 |
| 4 | 0 | 0 | 0 | true | accepted | 1 |

Revision totals after retries: 41, 82, 123, 164. In-flight request answered before
kill: false, false, false, true. Acknowledgement latency for 160 writes:
p50 15.8 ms, p95 42 ms, p99 76.6 ms. **Not F4 peer-render latency**, and not
S01–S19/T8 coverage on disposable cloud and self-host backends.

## Smoke and manual screenshot review

Started a source server on port 4499 with `MAUDE_NO_AUTOBUILD=1 CI=1` (sync
disabled, no linked-hub writes). `maude design runtime-health --port 4499 --quiet`
passed. Ran:

```sh
MAUDE_NO_AUTOBUILD=1 node cli/bin/maude.mjs design smoke --root /home/iagh/git/personal/maude --changed-only --out-dir /home/iagh/git/personal/maude/.design/_history/_smoke/followup-multiplayer-2026-09-22
```

No previous baseline existed, so the helper rendered all **74 canvases**:
automated **74 OK**, import graph clean; one viewport-escape advisory on
`ui/Onboarding.css`. All 74 PNGs were visually inspected, not sampled.

Manual caveat: `ui/Commands Overview.tsx` shows white background/black diagram
shapes and poorly legible labels rather than a correctly styled diagram. Its
stylesheet uses DS custom properties while the canvas imports only its own CSS;
missing token styles are a suspected cause, not a verified diagnosis. The canvas
and CSS are unchanged by this patch, but there is no prior smoke baseline to
prove when this started. This is an unresolved visual warning, **not a clean
visual pass**. No unrelated design edits were made.

Local (gitignored) report and PNGs:
`.design/_history/_smoke/followup-multiplayer-2026-09-22/`.

## Remaining blockers / next run

- **F1 (initial attempt, superseded below):** `bash .ai/scenarios/reliable-project-multiplayer/runners/local-e2e.sh --mode candidate --save-mode accepted`
  exits 1 before the matrix: “Build a bundled debug app first, or pass --app with
  its bundled executable.” Resume on the supported macOS native rig and run the
  complete matrix; an `--only L15` pass does not close the reservation.
- **F2:** supply an isolated R2 bucket and credentials for a real multipart
  round-trip through `apps/hub/src/s3.mjs`; do not substitute AWS-only/mocked
  results. No relevant credentials were present in this session's environment.
- **F3:** supply disposable cloud and self-host project access, then run
  S01–S19 plus T8 with cleanup. Local T32 above is supporting evidence only.
- **F4:** collect matched baseline/candidate peer-render timings on the native
  rig. Keep the 300 ms target unless the owner explicitly approves re-scoping.

The plan stays open; no `/flow:done` or archival is warranted by these results.

## Linux continuation

`surface-native.mjs` resolves the platform's actual debug layout and checks for
its staged runtime. Linux uses `maude-desktop` plus WebKitGTK and the embedded
WebDriver, with independent Chromium hub/peer participants. macOS still requires
its `.app`. Provenance records `linux-staged-debug`, `nativePlatform`, executable
hashes and resource manifests; Cargo caches are not treated as runtime resources.
This is not evidence about a signed release, Safari or WKWebView.

Build commands are documented in `../runners/README.md`. A Linux debug/no-bundle
Tauri build completed successfully. No release, deployment or version bump was
performed; the source server was compiled without regenerating the client/runtime
bundles. A generated comment-mount artifact was restored to its original bytes;
committed `apps/studio/dist/` remained unchanged.

Runs (gitignored evidence under `.ai/device/scenario-runs/reliable-project-multiplayer/`):

- `2026-09-22T11-45-21.457Z`: `--only L15` — **32 pass, 0 fail, 760 not-run**;
  source audit 144 parsed, no syntax errors or mismatches. Partial evidence only.
- `2026-09-22T11-47-30.179Z`: full run with `--samples 100 --soak-ms 1800000`,
  interrupted after the hub process exited unexpectedly around UI-text sample
  80. Subsequent peers failed both disk and UI observations. `cell.log` recorded
  only a null exit code; there is no proved cause or valid p95. The old runner
  lost the terminating signal, so local-cell now records code **and** signal and
  returns failure for a signalled hub exit. No product fix is claimed for this.
- `2026-09-22T12-00-07.261Z`: complete matrix retry with default one text sample
  and a requested 30-minute soak. Deliberately interrupted when the soak began,
  after **772 pass, 4 fail, 13 unsupported** (789 recorded rows), to diagnose
  failures before spending another 30 minutes on a non-certifying run.
  `L15.video.create` passed on **hub, native and peer**, as did every L15 timeline
  operation. Failed rows: `L18.css-undo.own-value` on hub,
  `L18.css-undo.peer-value-kept` peer-after-hub, and
  `L14.upload-video.play-and-seek` native-to-peers/peer-to-peers (native receiver
  probe timeouts; Chromium receivers passed). No completed soak or final source
  audit is claimed for this interrupted run. The test processes were stopped.
  Additional provenance limitation: F4 source edits were made after this run
  started. Initially loaded processes retain the old modules, but the L20 peer
  restart and fresh copies load the current source while the native binary
  stays unchanged. Treat this run as diagnostic evidence, not a matched-version
  candidate or latency comparison. A final certifying run must freeze all inputs.

F4 implementation: `suppressedEdit` publishes `source-written` only after a
changed write finishes, carrying its completed bytes. Accepted-mode sync feeds
that into the existing projection; it does **not** bypass hub validation or claim
acceptance locally. External writes retain the quiet window. Both new tests fail
without this change; the API plus real-hub suites pass **31/31** with it, including
one accepted action after a delayed watcher event. Peer-render measurements remain
open; no 300 ms claim is made.

F2 preparation: `scripts/dev/t18-r2-probe.mjs` requires an explicit R2 account and
scratch bucket matching `MAUDE_S3_*`, uploads 96 MiB through the real hub adapter,
hash-checks a streamed download and cleans up only its own exact object. It checks
for unfinished multipart uploads and fails if cleanup cannot be verified. **5/5**
probe tests pass, including a real loopback HTTP multipart round-trip (six parts)
and rejected-part/abort cleanup. This tests the probe, **not R2**; no vendor request
was made. Real credentials and an isolated cloud project are still missing.

### Additional Linux verification

- Native artifact/baseline/requirements and local-cell exit tests: **21 pass**.
  The custom executable-name manifest test failed before the manifest included
  the actual selected filename, then passed after the fix.
- Full sync lane after the F4 test: **1,202 pass, 1 fail**, 70 files, 9,093
  assertions, 264.60 s. The unchanged structured-actions reversal/determinism
  case timed out at 20 s while native E2E and smoke were also running. Its file
  passed **6/6** alone with the same 20 s limit (38.12 s total). This supporting
  rerun does not turn the earlier whole-suite result green. A subsequent full
  rerun completed **1,203 pass, 0 fail**, 70 files, 9,093 assertions, 223.67 s,
  still with the original 20 s limit (`/tmp/maude-linux-followup-sync-rerun.log`).
- Studio `bunx tsc --noEmit --pretty false`: exit 0. Desktop E2E typecheck
  (`bunx tsc -p ../desktop/e2e/tsconfig.json --noEmit --pretty false` from studio)
  exits 2: nullable title at cloud-attach lines 306/307, element return type at
  git-lifecycle line 51, missing webdriverio type at shell-parity line 2. These
  files are unchanged; the modified multiplayer scenario has no reported error.
- Second smoke: **74/74 automated OK**, all 74 PNGs read, under
  `.design/_history/_smoke/followup-multiplayer-linux-2026-09-22/`. The Commands
  Overview warning remains; Photo Editor Trailer also has a white/serif-looking
  frame and Maude Explainer a dark empty video frame. Their cause/timing is not
  diagnosed by a still screenshot; no clean visual certification is claimed.
  The owned CI-mode server on port 4499 was stopped after capture/review.

The full native run exposed two media-play probe timeouts on the native receiver:
its video was decoding and advancing, without a media error, but the debug bridge
waited only 1 s for `play()` to settle. The bridge now permits 5 s for that one
asynchronous operation; ordinary DOM probes retain 1 s. A real isolated-browser
bridge test with a controlled 1.2 s media-start promise fails without this change
and passes with it; rejected and permanently pending promises still fail. This
test proves the bridge deadline, not Linux media playback. A rebuilt native
rerun was then performed as described below; the earlier matrix used the original build.

### Frozen-input focused native rerun

After stopping the full diagnostic run, the current server was compiled and
the Linux debug application rebuilt (no client/runtime bundle regeneration;
the incidental comment-mount output was restored). No source edits were made
during the next run:

```sh
PATH=/home/iagh/.cargo/bin:$PATH MAUDE_NO_AUTOBUILD=1 bash .ai/scenarios/reliable-project-multiplayer/runners/local-e2e.sh --mode candidate --save-mode accepted --only L06,L18.css-undo,L14.upload-video,L15
```

Evidence: `.ai/device/scenario-runs/reliable-project-multiplayer/2026-09-22T12-38-47.290Z/`.
Driver completed; runner exit 2; **64 pass, 1 fail, 727 not-run**.
Source audit: 183 TSX files/snapshots parsed, zero syntax failures, final text
mismatches or missing snapshots. No full coverage or soak claim.

- Every `L15.video.create` and timeline case passed on hub/native/peer.
- Every `L14.upload-video.play-and-seek` passed, including the actual native
  receiver's advancing playback and red/blue decoded-frame checks.
- Five of six CSS-undo rows passed. `L18.css-undo.own-value` failed for the
  native author: none of the three disks returned to the expected prior value.
  Unlike the earlier full diagnostic run, the hub's own-value undo passed.
  This remains an intermittent blocker, not a fixed product defect. The web
  trace now records successful edit responses, record-edit receipt and undo
  key receipt without payload/source/token contents. Native ordering still
  needs to be isolated; do not replace the failing assertion with a sleep or
  silently mark it unsupported.
- F4 UI text samples all converged. The six receiving-peer observations were
  408.6, 453.5, 548.9, 549.8, 571.4 and 711.1 ms; disk arrival was 71.3–110.3 ms.
  These are **individual observations**, not a p95 estimate or a matched
  before/after experiment. Completed-write delivery alone does not establish
  the 300 ms peer-render target; the renderer/HMR segment needs investigation.

All owned runner/server processes were stopped by cleanup. `apps/studio/dist/`
is unchanged. Final hash comparison against the focused run's source manifest:
**1,895 files checked, zero changed**. Changed-file Biome: 19 files, exit 0,
two warnings on unchanged lines (`noDelete` / `useOptionalChain`), no edits.
The plan and graph checkpoint remain partial with F1–F4 open.
No commit, archive, version bump, deployment, production write or remote KG sync
was performed in this Linux continuation.

## Goal continuation: actual R2, cloud crash oracle and undo HMR — 2026-09-22

This section supersedes the access blockers above, not the full-certification
requirements. The owner authorized new isolated Cloudflare resources and
connected the Cloudflare MCP; bucket-scoped R2 credentials were supplied in a
local private file, never committed or copied into this report.

### F2 complete: production adapter against actual R2

Created `maude-multiplayer-test-20260922` in the owner's Cloudflare account.
`scripts/dev/t18-r2-probe.mjs` ran through `apps/hub/src/s3.mjs` using its normal
64 MiB multipart threshold and 16 MiB parts. **96 MiB, 6 parts, passed**, upload
38,239 ms, streamed download hash equal to the random input. Cleanup asserted
an empty run-specific prefix and no incomplete multipart upload. The empty
bucket is retained for subsequent isolated tests; no production bucket changed.
Machine-readable result: [R2 proof](./followup-2026-09-22-r2.json).

### F3 supporting proof: real Cloudflare Durable Object

Bundled the existing `scripts/dev/t32-store-worker/worker.mjs` with Bun and
deployed `maude-t32-test-20260922` with its own SQLite-backed ProjectStore
namespace and random bearer secret. Ran `t32-verify.mjs --n 40 --rounds 4`
against `/t/followup-20260922`, with the token supplied through the environment.
Each round SIGKILLs the local hub and starts a fresh local data directory;
only the actual cloud store survives. **All four rounds passed**, 0 lost ACKs,
0 duplicate revisions/transactions, one effect per retried in-flight action,
last acknowledged hash preserved. 160 ACKs: p50 265.7 ms, p95 404.1 ms, p99
509 ms — these are remote durable ACK times, **not F4 peer-render latency**.
Machine-readable result: [cloud crash proof](./followup-2026-09-22-cloud-crash.json).
After completion the temporary Worker and its DO namespace were deleted via
Cloudflare; its former endpoint returned 404. This is the T8 crash subset,
**not** S01–S19 application acceptance on both backend families.

### F1 undo root cause and focused verification

Diagnostic run `2026-09-22T13-16-15.341Z`: 7 pass, 1 fail. The failing peer
conflict row sent undo, received an HTTP 409, then reported `the canvas closed`
instead of the conflict: HMR unmounted CanvasShell while the parent's reply
was in flight. A native-shell diagnostic recorded only timestamps, HTTP status
and fixture font weights; that temporary instrumentation was removed afterward.

Two deterministic lifecycle tests failed on the old effect (`the canvas closed`
instead of the real success/conflict). Cleanup now drains already-sent requests
until their parent response or bounded timeout, still rejecting non-parent
messages and refusing new writes from the disposed mount. A separate test
proved the replacement UndoStackProvider could concurrently undo the same entry
before the old provider's ACK. Pending operations now serialize per canvas
across the HMR handoff and re-read persisted state at each operation boundary.

`bun test test/use-undo-stack.test.tsx test/canvas-edit-ack.test.ts
test/undo-stack.test.ts test/edit-source-command.test.ts`: **42 pass, 0 fail**.
Studio typecheck passed; changed-file Biome passed with one pre-existing warning.
The rebuilt Linux debug app's focused run `2026-09-22T13-23-33.982Z` completed:
**8 pass, 0 fail, 784 not-run** (runner exit 2 correctly refuses certification of
partial coverage). It includes all six CSS-undo directional rows and bootstrap
checks. This is not yet proof of the formerly intermittent own-value case over
the full matrix.

Full frozen-source run `2026-09-22T13-26-38.718Z`, with
`--mode candidate --save-mode accepted --soak-ms 1800000`, ended **764 pass,
12 fail, 13 unsupported, 3 not-run**. All video and CSS-undo rows passed.
Chromium disappeared during L16 at 13:58:23 UTC; subsequent UI checks reported
a closed browser, and the soak was not reached. Final 165-file disk parity
passed; 411 source snapshots parsed without errors and nine comparisons had
no mismatch. These are not substitutes for the failed UI coverage.

The focused L16 run `2026-09-22T14-07-48.265Z` ended **14 pass, 0 fail,
6 unsupported, 772 not-run**, with a normal browser shutdown (exit 0).
Browser disconnect, page-close and crash telemetry now distinguishes intentional
cleanup from an unexpected exit. The full-run disappearance is not explained
by this focused success. F1 remains open.

### F4: renderer measurements and delayed HMR deduplication

Before run `2026-09-22T14-10-28.882Z` and after run
`2026-09-22T14-18-26.167Z` each exercised ten UI text edits per author lane:
32 passing rows, no failures, partial coverage. Across 60 receiving observations,
before p50/p95/p99 were **449.0/616.1/1066.5 ms**; after they were
**487.0/645.8/712.6 ms**. Overall latency improvement is **not established**,
and the original **300 ms p95 target remains unmet**. These local samples are
not cloud ACK measurements or complete matched-network certification.

The intermediate three-sample trace showed a delayed duplicate remote HMR
broadcast for the same disk version. The fallback interrupted an already
running module reload. The broadcaster now suppresses only repeated remote
module/CSS events with the same device/inode/size/nanosecond mtime/ctime tuple;
a changed version still broadcasts and stat failure fails open. The regression
test failed before the fix (two broadcasts instead of one); 32 focused HMR
tests and studio typecheck pass afterward. Warm receiving-hub edits now show
one broadcast to each of its two sockets, not a second delayed broadcast.
This removes duplicate work but does not close F4.

### F3: actual cell, accepted mode and native sign-in investigation

The isolated cell is live with a real Cloudflare container, ProjectStore DO,
private R2 bucket and separate canvas origin. Resource identities and teardown
scope are recorded in [the cell manifest](./followup-2026-09-22-cloud-cell.json).
The owner switched the test project to accepted revisions through the dashboard,
then created `ui/Cloud acceptance.tsx` through the studio UI. The invited
designer's independent browser rendered the board. History returned revision 1,
`canvas.create`; its actor was `cell-loopback-sync`, so individual author/own-undo
semantics are **not proven** by that create. The deployed cell image predates
the local HMR fix and must not be treated as its verification.

Clean Linux-native cloud-entry runs reached the team sign-in button but then
timed out. A diagnostic run (`cloud-entry-2026-09-22T14-37-38.007Z`) established
that the native main thread was in `do_wait`, with a child `xdg-open` also
waiting on its browser. The production `open_cloud_url` was a synchronous Tauri
command calling `open::that`; the library explicitly documents that some
launchers block. The diagnostic run was intentionally stopped after this
evidence, not recorded as a pass. An asynchronous command with a blocking-pool
launcher preserves URL validation and launcher errors while freeing the event
loop. All 11 existing URL-policy tests pass. No authentication or authorization
bypass was added.

Native run `cloud-entry-2026-09-22T14-43-16.173Z` subsequently passed in
50.7 seconds: real login approval, project pick, managed copy, explicit sync
consent, canvas create, leaf-editor text change and native persistence/render.
The independent owner browser then rendered **Native cloud UncRL8 edited by
designer**; accepted history returned revision 4/action
`a_23568527-e5bd-4f0c-9485-35342a50687d`, actor
`designer-a@maude-f3.invalid`. All five native screenshots and the peer
screenshot were inspected. This completes the **cloud S01 slice**, not the
remaining backend matrix. Machine-readable evidence:
[cloud native entry](./followup-2026-09-22-cloud-native-entry.json).

The preceding create-only run (`cloud-entry-2026-09-22T14-40-02.671Z`) passed
its assertions but screenshots exposed a consent modal still covering the
canvas. Its synthetic driver clicked behind that modal. It is **not** S01
acceptance evidence; the later test explicitly accepts consent, checks the
dialog closes, waits for the real canvas and edits its text. A type error in
the new row-count predicate was corrected afterward; E2E typecheck now reports
only four existing errors in unchanged scenarios. Changed-file Biome passes.

A new full frozen-source candidate run `2026-09-22T14-45-34.338Z` was started
with 100 text samples per author and a 30-minute soak, compared against the
preserved `2026-09-16T20-10-34.675Z` baseline. Browser process exit code/signal
is captured through Playwright's public server handle, with loopback-only RPC
and no capability-bearing DEBUG logs. Inspect its terminal result before making
any F1 claim. Inputs stayed frozen until its deliberate termination described below.

All cloud resources remain isolated and retained for further F3 coverage;
cleanup is pending. No archival, commit, push or production deployment has
been performed.

### Browser personal-history limitation — confirmed, not a new regression

Read-only requests from the real owner and designer browser sessions both
marked the cell-loopback revision as `mine:true`, and the designer's native
revisions as `mine:false`. A new owner-created `ui/Browser ownership probe.tsx`
was accepted at revision 5/action `a_c198c6cd-382d-4b77-af08-ebd0fed6fefd` under
`cell-loopback-sync`. The designer's attempted personal-undo request returned
**404**, and the accepted history remained unchanged. No other person's action
was undone. The test canvas remains disposable fixture content.

This is the **existing explicitly unsupported cloud-browser personal-undo
case**, documented in the parent plan's 2026-09-15 checkpoint, the hub's
`studio-manifest.mjs` refusal and the UI's `cellManaged` guard. It is not an
unexpected authorization bypass. Do not remove the refusal to manufacture a
passing test; do not claim browser per-person attribution is implemented.
F3 still requires the supported native personal-undo paths against both real
backend families. The designer History panel was inspected in
`.ai/browser/screenshots/followup-f3-history-wrong-personal-owner.png`.

### Real-cloud concurrency, fencing, personal history and a restore failure

[Cloud protocol evidence](./followup-2026-09-22-cloud-protocol.json) records
independently authenticated designer A/B proposals, accepted revisions/hashes,
browser observations and limitations. S03's title/color stale-base merge passed
at revision 8; the owner browser rendered the merged title in red. A's personal
undo produced revision 9 and kept B's red. In an ABA sequence, B's later equal
title remained protected: A's undo and stale redo returned 409; B's attempt to
undo A's action returned 403. Revision 12 and history stayed unchanged. These
are protocol subsets with browser receiving proof, not two native author flows.

S05 was repeated against revision 12 with two real authenticated WebSocket
connections and a fresh third reader: explicit raw Update and SyncStep2 frames
(3,065 bytes each) did not alter the accepted source/head/log. A 52-byte presence
state reached the witness. Invalid TSX returned 422/source-invalid and epoch 0
returned 409/epoch-stale. Loopback and dependent repair/restart remain untested.

**S11 has a reproduced product failure.** The cloud history preview correctly
renders revision 8 beside revision 12. Clicking **Restore saved version** inside
that preview and accepting the confirmation instead calls `/_api/git/discard`,
which returns 409; revision 12 and its hash remain unchanged. `app.jsx` wires the
preview's restore callback unconditionally to `gitDiscard`, discarding the
selected accepted revision. By contrast, **Restore on history row 8** correctly
creates revision 13/action `a_6a0faf7b-9615-44bc-bc2a-7302f1ae2e89` with
kind `history.restore`; its durable hash and rendered title match revision 8.
The row's transient success-text wait did not complete, so the oracle is the
accepted action, exact source hash and browser render, not that wait. All five
new screenshots were inspected. The subsequent fix and remaining verification
are recorded below; this original failure is retained as RED evidence.

### F4 measurement checkpoint — provisional, not certified

The interrupted full run completed 100 text samples per author, with 600 peer
receiving observations. Recalculation using the canonical nearest-rank method
(`surface-statistics.mjs`) gives provisional 95th order-statistic values in ms:
hub→native 606.9; hub→peer 418.2; native→hub 589.7; native→peer 460.5;
peer→hub 602.5; peer→native 564.0. These exceed 300 ms. Earlier commentary's
418–656 ms range used a different floor-index convention; these are the
canonical-method values. **The driver ended unsuccessfully**, so certified
percentiles remain disabled even though the final source audit has no mismatch.
There are only 100 observations per direction, not the 1,000 required
by the runner for p99. Earlier 10-sample before/after figures are exploratory
order statistics, not qualified per-direction p95 or matched three-pass proof.
No latency target has been changed or marked achieved.

### Current full-run L07 failure — live diagnostic checkpoint

The same full run later reached 1,043 rows with **4 failed**, 6 unsupported and
1 not-run; the driver remains live, so this is not its terminal count. All four
failures are in peer-origin L07: duplicate timed out while `seedCanvas` awaited
initial cross-client disk equality; insert/resize/move then reported the absent
native `ui/SurfaceEl-peer.tsx`. Delete was not run because its duplicate
prerequisite failed. These are not four independently demonstrated UI bugs.

Read-only inspection while the driver continued found the exact 400-byte source
on hub, peer and fresh-1 disks, all SHA-256
`5567a5b397ee8691635d0776ac3ae3c618e133949b0313f1ddfc6665e33908ac`.
The native file was absent, but its persisted Y.Doc held those same bytes/hash.
Native `/_sync-status` still listed `ui-surfaceel-peer` as connected with zero
pending docs/queued operations. Thus transport reached the native Y.Doc while
the expected native source file was missing; the responsible materialization
or lifecycle race was not yet identified at that checkpoint. No resync, manual file repair,
restart or runtime-source edit was used to conceal or alter this run's failure.

### Terminal full run and regression fixes

The run above was deliberately interrupted through its WDIO process after four
verified failures; this was not a wait timeout. Its runner exited 1 and cleaned
its owned processes. Final rows: **1,068 pass, 4 fail, 13 unsupported, 1 not-run**.
The long soak did not complete. Source audit: 1,608 parsed, zero syntax failures;
900 compared, zero mismatches or missing snapshots. These do not certify the UI.

L07's native source was found at `ui/surfaceel-peer.tsx`, not the expected
`ui/SurfaceEl-peer.tsx`. The native Y.Doc and all four copies had identical source.
An author can open its transport room before doc.create commits. Discovery used
that room before the accepted manifest had a path, pinning the lowercase legacy
fallback. `withAcceptedDocs` now enumerates only live manifest documents when
accepted saving is active; legacy discovery is unchanged. The new real-hub
`sync-accepted-runtime.test.ts` case reproduces that ordering, failed without
the fix, and passes with it. Four affected sync/path files pass **68/68**.

The history-preview DOM regression opens r8, selects r9 and clicks Restore. It
failed because only the filename reached the callback. DiffView now passes the
current picker revision, excludes Git HEAD for accepted history and describes a
new saved version. App routing loads accepted history and shares the project
restore endpoint with history rows; invalid revisions cannot fall back to Git
discard. The separate routing guard also failed before the change. Five affected
history/posture test files pass **47/47**; studio typecheck passes. Scoped Biome
passes with one pre-existing optional-chain warning in sync/index.ts.

The surface harness now adds `L18.history.preview-restore` for hub, native and
peer: open one revision, select another, restore through the preview, require a
new history.restore action and verify source plus rendered text on every peer.
Its real UI result and the next complete certification remain pending. These
are implementation checkpoints, not closure of F1/F3/F4.

### Focused native/browser regression verification

[Machine-readable local results](./followup-2026-09-22-local-regressions.json)
retain four runs. `2026-09-22T15-48-31.013Z` passed **20 rows**, failed zero and
excluded 775: all 15 L07 operation/direction combinations plus three preview
restores and bootstrap. Its preview screenshots were too early to prove preview
rendering and the history list did not show the new restore action.

A separate DOM regression reproduced the stale list; the fix invalidates its
cached scope on successful restore and applied project revision. The affected
history/posture suite is now **48/48**; studio typecheck passes. Two isolated
follow-up runs failed before opening the preview because the test helper assumed
the dock was already open. This was a harness dependency on earlier L07 actions,
not evidence that preview rendering failed. The helper now opens History through
the always-available `open-changes` button; it does not refresh the page or retry
a product mutation.

The strengthened isolated run `2026-09-22T15-58-35.918Z` completed with **5 pass,
0 fail, 790 excluded** (three author directions and bootstrap). It waits for both
actual preview documents to render different titles, changes the selected
revision, accepts Restore, requires a new history.restore action and visible row,
and checks every participant's rendered text and exact source. Hub 3→5, native
6→8 and peer 9→11 were restored from previews initially opened at 4/7/10. All 12
preview/result PNGs were inspected: selected old/current content and final
restored titles are visible; history lists the new action. The native first-run
collaboration nudge remains visible but does not cover the tested controls.

Both successful runners exit 2 because the excluded full matrix is explicitly
incomplete; their inner drivers completed with exit 0. No full certification or
real-cloud updated-build verification is claimed. Runtime bundles were rebuilt
release-minified and the Linux native debug app restaged before these runs.

Full candidate `2026-09-22T16-00-42.733Z` is now running against the preserved
`2026-09-16T20-10-34.675Z` baseline, with 100 samples per author and the 30-minute
soak. Source/build inputs remain frozen. Inspect its final driver, source audit,
baseline comparison and UI results before making any completion claim.

### F3: updated isolated cloud image and selected history restore

Candidate `test-20260922-r2` was built with the existing frozen-install hub/cell
Dockerfiles and pushed under a fresh tag (the earlier image was not replaced).
Cloudflare application version 2 is running in ams22; the served client hash
matches its sealed build. The restarted cell preserved revision 13, epoch 1
and all five documents. Production resources were not changed.

The owner opened preview r13 and selected r6 (visually distinct old/current
titles and colors). While that preview remained open, designer B changed the
heading's title attribute in the actual inspector, producing r14. The owner's
history refreshed while its picker stayed on r6. Actual preview Restore and
confirmation then produced history.restore r15. The accepted HTML hash equals
r6's HTML effect, both browsers render the restored title/color, the preview
closes, and the new history row is visible without refresh. Peer r14 remains
readable in history. All four screenshots in the new cloud-history JSON were
inspected. The first read-only oracle mistakenly selected the doc.create effect
with null afterHash; the corrected HTML-effect hash assertion passed.

This is **partial-with-failure**, not S11 completion: designer B's unfocused
inspector title input still showed the pre-restore value while the actual h1
(same data-cd-id) and accepted source had `Original title`. An explicit heading
click refreshed the inspector. Soft HMR remount currently does not repeat the
initial `loaded` handshake that schedules shell selection readback; this is an
investigation lead, not yet a tested fix. Runtime inputs remain frozen while the
full candidate runs. Media history/native-cloud variants, this stale-inspector
regression, other S01-S19 requirements and fixture cleanup remain open.

### F3: media history survives a real isolated-cell restart

Authenticated fixture writes created `ui/F3MediaHistory.tsx` at r16 and changed
its referenced image at r17. Actual owner history UI preview showed distinct
old/current images; selecting r16 and confirming Restore created r18. Both
browsers rendered the old image, and current HTML equals r16's exact hash.
Both browser sessions were then closed and only `f3-cloud` was restarted through
its authenticated operator route. A fresh process restored both assets from R2
(2 restored, 0 failed). Head 18/epoch 1, r16/r17 historical bodies, r18 action ID
and both image SHA-256 values survived. Direct signed R2 GETs returned the exact
bytes, not merely object names or a success counter. Reopened owner and designer
browser screenshots both show the old image; all six linked PNGs were inspected.

See `followup-2026-09-22-cloud-media-history.json`. The helper's cross-frame
`wait --text` timed out; fresh recursive snapshots and independent screenshots
show the image loaded. The cell's `/health` reports readiness and restored
assets; `/_health` is the separate authenticated studio route and is not an
operator-health substitute. No secret values enter this report.

Full candidate `2026-09-22T16-00-42.733Z` remains live. Its two hub-side L18
history rows timed out while the native/peer preview directions passed. No
source/build/test input has been changed during the run. These failures and the
separate stale-inspector issue remain open; this media subset does not certify
F1 or all of F3.

### Prepared stale-inspector regression/fix — isolated copy only

To preserve the live full run's source seal, copied the studio source to
`/tmp/maude-hmr-fix-vzwuAZ/apps/studio` with the existing studio dependencies
shared read-only. No main-tree production/test/build input changed (all 1,900
manifest file hashes were rechecked unchanged). This copy is not deployed.

New colocated `test/canvas-hmr-loaded.test.tsx` mounts the real React runtime,
keeps the same element ID, replaces its title and captures the parent handshake
with the DOM value visible at delivery. It first failed because the replacement
DOM was correct but no loaded announcement was emitted. The candidate emits the
existing loaded handshake from the successful commit callback, only on a soft
replacement with a file identity, never on a failed render. The test also proves
that the last good canvas remains visible after a render exception and a later
valid replacement announces its committed DOM.

Candidate HMR/ordering suite: **7 pass, 0 fail**. Studio typecheck exits 0 and
changed-file Biome check passes in the isolated copy. The new test now suppresses
only its expected React error logging while asserting that the intentional
exception was reported. The existing boundary test still prints its expected
exceptions. Candidate source SHA-256:
`b96fb4413176933502bc3a101c3644c2d49233bfecc65767db1684296d402788`;
new test SHA-256:
`985d418b96481539f569a72c4dff1a50072d03f50b41fd1a27627c8801471eed`.

Transfer/re-test in the main tree only after the full run terminates, then rebuild
and verify receiving-inspector behavior in actual UI. No passing cloud fix is
claimed yet. A preliminary command from the wrong cwd also discovered staged
desktop copies of the test and failed their dependency resolution; the explicit
isolated-copy run is the reported 7-test result. An attempted `pnpm exec` in the
copy auto-installed there and failed its install hook; subsequent static checks
used the already-installed repository executables, with no dependency changes
to the main tree.

Read-only diagnosis of the full run confirms the hub studio API still returns
the two expected versions for both failing history canvases (974/975 and
982/983). The raw project API accepts `entry`, not `path`, as a history filter;
do not mistake its unfiltered global results for per-canvas history. Missing
UI failure screenshots/step detail prevent attributing these timeouts yet.

### Isolated fixes verified through actual cloud-connected UI

Further actual-cloud reproduction identifies a concrete panel-state bug: after
selecting Inspector, the status-bar Changes button sets its pressed state but
leaves Inspector selected and History hidden. The callback directly sets
`changesOpen`, bypassing the established same-dock exclusive opener. The isolated
copy now uses `openRightPanel('changes')`. A wiring regression failed before the
one-line fix and passes afterward; the five-file history suite passes 50 tests.
Actual fixed UI opens History and renders rows over the previously open
Inspector. This explains a mechanism for the hub-only full-sequence timeout;
the full native runner still must recheck it rather than assume certification.

The same isolated client was built release-minified and connected as designer A
to the actual cloud project. Its inspector changed a title attribute at r19.
The owner's cloud preview selected r15 and confirmed Restore, creating r20.
Without any receiver reload or reselection, its still-selected inspector's
unfocused title input became `Original title`. Cloud accepted HTML, restored
history effect and local disk match SHA-256
`2616f93c7c6df9ac5ae7aff381ea24f92c7164667209af8970d6045f7ca1c39c`.
Revision 19 remains readable. Both served bundle hashes match the isolated
release outputs. All four before/after screenshots were inspected.

See `followup-2026-09-22-cloud-ui-candidate.json` for exact source/build seals,
action IDs and screenshots. This advances the candidate fix beyond a unit
test; it is not main-tree, packaged native or new cloud-cell-image certification.
Main source/build inputs remain frozen while the full run completes its soak.

### Full run terminal; candidate fixes transferred after seal release

`2026-09-22T16-00-42.733Z` completed its driver (exit 0) and the outer runner
returned 1 for the baseline regression: **1,077 pass, 2 fail, 13 unsupported,
0 not-run**. Source seal `a8023dcfd56b6f862ef3e209efdde3e1514c8c614c351cf44e21cf7be47ee457`
remained unchanged through completion (1,900 files). Both failures are hub L18
history timeouts. Baseline comparison: 777 held, 1 regression, 1 repair, 300 added.
The 30-minute mixed-session soak completed 750 edits and 250 media files with
zero misses; lifetime-process RSS sum grew 1.318×. Its p95 observation was
886.2 ms, not a calibrated ordinary-edit percentile. Fresh reopen passed across
four participants with 424 eligible files and no differences. Source audit:
1,617 parsed, zero syntax failures, 900 comparisons, zero missing/mismatched.

Only after authoritative termination, transferred the four isolated candidate
source/test files. In the main tree the two new regressions failed for the same
expected reasons (no loaded announcement; direct panel setter), then passed
with the two production fixes. Affected suite: **57 pass, 0 fail across 8 files**.
Studio typecheck and three-file Biome check pass. Release build retains committed
runtime packages and reproduces the exact client/comment-mount hashes recorded
in the isolated candidate JSON. Existing boundary tests print their intentional
React exceptions; the new test scopes its expected-error capture.

The actual native preview scenario now selects each participant's Inspector
before opening History and requires receiving font-weight controls to return
from 700 to 400 after the remote restore, without reselection. Its history
oracle also matches the action's document effect instead of passing an ignored
`path` parameter to the raw project-history route. These are stronger checks,
not retries or a bypass around the status-bar failure. Native focused/full
results are still pending.

Native focused attempts remain visible rather than being overwritten:
`17-12-51.627Z` passed hub preview/restore and actual native/peer inspector
readback, but two following directions failed their Inspector setup. The next
`17-14-43.799Z` passed hub and peer directions but failed one setup wait.
Diagnostic run `17-17-15.552Z` pinned that wait to **current-inspector on hub**
before the native author's restore: live heading was `Preview current native`
at weight 700, while the reopened Inspector retained the previous canvas's
selection at 400 (no current heading halo). Its JSON and screenshot were read.
The selection helper had treated an enabled old knob as an already-selected new
target. The setup now explicitly selects the new heading, before the stimulus;
post-restore receiver interactions remain prohibited. Existing auto-open logic
deliberately does not steal focus from an open History panel, so the setup also
selects the Inspector tab explicitly. No production change or timeout widening
was made in response to these setup failures.

The e2e file's optional visibility return was made explicitly boolean; its
typecheck now reports only the four previously existing errors in unrelated
cloud-attach/git-lifecycle/shell-parity scenarios. The final fixture edit was
formatted and its one-file Biome check passes. `pnpm exec tsc` at the repository
root had no executable; the actual check used the installed studio tsc binary.

### Verification-loop pause: selection identity, not an enabled control

`17-19-34.635Z` still failed native-to-peers at the hub's pre-restore Inspector
setup. A DOM `element.click()` did not deliver the pointer sequence the canvas
selection layer consumes. The third precondition adjustment used the existing
debug probe's full `pointer` gesture. `17-22-31.678Z` then completed **3 pass,
3 fail, 1 unsupported, 788 not-run**: each preview direction failed its
`current-inspector on hub` precondition; ordinary history Restore still passes.
The three JSON diagnostics and all three failure screenshots were inspected:
each current h1 renders at weight 700, but the Inspector and halo belong to its
surrounding section. `selectHeading` gates on the old knob's availability and
can return before the asynchronous selection response disables that knob.
This requires reviewing the selection helper's actual target-identity contract,
not widening the 15-second timeout, restoring via an API, or weakening the
post-restore Inspector assertion. The execute loop is paused after its third
unsuccessful preparation adjustment; no fourth speculative edit was made.
Source seal of the last run:
`68bdb4858fbdbd31b3aa5e2dc0febbab91829cc3dfb43c772d9ffc276c6df623`
(1,901 files). All local surface processes have terminated; the earlier
isolated candidate browser and its dedicated studio process were closed.

The production fixes and their fail-without tests remain in the main tree.
The positive hub-origin receiving-native/peer subset from `17-12-51.627Z` is
retained, not promoted to all-directions certification. No new full run has
started with this unresolved preparation failure.

### Prepared cloud image r3 — pushed, not rolled out

Built the actual hub and derived cell through their existing Dockerfiles with
frozen installs. The immutable tag
`registry.cloudflare.com/b5b596efe65abb732777c7171dc18145/maude-followup-cell:test-20260922-r3`
was pushed; the first push ended with `blob unknown to registry`, and an exact
retry succeeded with digest
`sha256:6c3c46886d6a0ae947527976e09b57ac4a30be9e4a2a417c94bcfc8c2ca46b58`.
Image size is 225,643,250 bytes. Container's own sealed build manifest:

- client: `01ca7ed2497a23d6dbe598d1f21f3214fd4361292d3cef507d1498e98e1a5522`
- comment-mount: `b1efbefd8352a85fbeef1d632abc9704f0d0cbb1291f35eb09a64d4f1ee8ba48`
- styles: `b82026c747519579800c6cb85e465b0145ac90cb6e3795a32ce0821fabd6bc69`

The Dockerfile uses its pinned Bun 1.3.3 builder; these are the actual container
outputs, not the different local Bun 1.4.0 bundle hashes. Deployment must verify
against this container seal. The six fixture documents and epoch 1 were read
before any rollout; no rollout was performed and the active cell remains r2.
Registry credentials were kept in a mode-600 file in the private fixture
directory, issued for 30 minutes; no credential values appear in evidence.

### Selection helper review and all-direction native Restore

The helper's old premise was false: an enabled font-weight knob could belong
to the previous canvas, while a bare pointer selected the enclosing section.
The product already provides Cmd/Ctrl+click for direct leaf selection
(`input-router.tsx`); no extra drill or product selection behavior was needed.
The helper now clears the old selection through its visible control, selects
the current heading with that gesture and verifies the stamped ID and text in
the status bar plus the `h1` Inspector tag and enabled knob. This is exclusively
pre-stimulus setup; receivers are still untouched after Restore.

- `2026-09-22T17-34-27.762Z`: 5 pass, 1 fail, 1 unsupported, 788 not-run.
  Selection and restored content/receiver readback succeeded in every direction;
  native timed out at the subsequent new-history-row check. Its four preview
  and restored screenshots were inspected; the native panel showed loading.
- `2026-09-22T17-36-54.896Z`: 6 pass, 0 fail, 1 unsupported, 788 not-run.
  Only failure diagnostics changed between these runs. Preview selection,
  restored source, new action/visible history row and both receiving Inspectors
  passed for hub, native and peer authors. All twelve preview/restore PNGs were
  inspected. No post-Restore reselection or reload occurred.

Scoped Biome passes. This fixes the selection precondition, not a demonstrated
history-loading root cause: the earlier intermittent 15-second timeout remains
recorded. New failure diagnostics capture the history panel and completed
history-request timing at the failed row. Full F1, real-backend F3 and F4
performance acceptance remain open; no archive, repository commit or push.

### Full-run L06 regression: selection readback must not tear down typing

Run `2026-09-22T17-38-58.932Z` was stopped via the runner's SIGINT cleanup after
83 pass/8 fail. It terminated with exit 130 before completing L06; no soak,
baseline certification or percentile claim. The 1,901 source inputs matched
seal `eee219888c15c3ad058e1443c3f88ccb1fa544cdae79fe6ab7e5e2c9b8c5b6ca`
at termination. Every failure was native inline editor entry/commit (including
samples 3/7/9/21 failing entry and 12/13/14 losing the editor before Enter).

The successful-HMR handshake invokes the existing halo-restore selection
readback. CanvasRouter's inline-edit effect depended on the whole `selSet`
snapshot, so each same-element readback disposed the active text session.
`canvas-text-selection-refresh.test.tsx` mounts the real CanvasShell, opens a
heading editor, types, sends the same select-by-id and then commits. It failed
with contenteditable null after the refresh. Its standalone unrelated AI/export
notification service is an empty local fixture, not backend/sync certification.
Initial test-harness failures (missing notification socket, then happy-dom's
Response being inappropriate for Bun.serve) were corrected before the actual
RED assertion; they are not counted as evidence of the product bug.

The effect now depends on the host lifetime and reads the current selection via
a ref (same pattern as its existing undo-stack ref). Afterward the editor and
typed bytes survive, Enter emits exactly one edit, and Escape cancels a second
entry without another edit. Affected regression run: **71 pass, 0 fail in ten
files**; studio typecheck passes. Scoped Biome has one existing optional-chain
warning outside the changed lines, no errors. Native verification remains
required, as does deploying this newer change (the current cloud r3 predates it).

### Actual isolated r3 rollout

Only test application `a03073b4-13ac-42ee-8b75-b58b598e3240` was rolled through
`21f58900-a5ae-46d1-8dad-035328e52490`. It reports version 3, one active instance,
no health errors; the served client, comment-mount and styles hash-match the
container manifest. Authenticated before/after bootstraps are exactly equal,
including all six document records, revision 20 and epoch 1. Full proof and
limitations: `followup-2026-09-22-cloud-r3.json`. Production is untouched;
test fixture cleanup still awaits backend certification.

### Native text lifetime verification and remaining selection race

`2026-09-22T17-48-16.419Z` (`--only L06.ui-text-edit,L07,L18 --samples 100`)
terminated with 334 pass, 1 fail, 1 unsupported and 756 not-run. All 300 text
edits passed, including 100 native-author edits. L07 passed all 15 operations;
all three preview restores and the ordinary Restore passed. Selected/restored
revisions were 353/355 (hub), 356/358 (native), 359/361 (peer); both receiving
Inspectors updated without post-Restore clicks. All twelve preview/restore PNGs,
the three views of native text sample 100 and the failing selection screenshot
were inspected. This does not resolve existing layout/a11y warnings or certify
the full matrix.

The source seal remained
`117c1a6139fcd9b6d16e14dbf0b2f801425be951438fea869a6d40867973cc8e`
across all 1,902 inputs. Source audit: 1,362 snapshots parsed, zero syntax
failures, 900 exact comparisons and zero mismatches/missing copies.

One CSS-undo preparation failed on the peer during native-after-peer: heading
`0b1b61f6` existed, but the shell had neither a selected chip nor Inspector tag
after the click. No undo was attempted in that row. The screenshot shows the
correct heading with no local selection. Its cause is not established by the
screenshot. Added a bounded read-only trace of active-frame selection/loaded
messages and before/after heading DOM; gestures, deadlines and assertions remain
unchanged. Scoped Biome and the real-browser frame-probe test pass.

Diagnostic run `2026-09-22T18-00-46.437Z` (`--only L18.css-undo`) terminated:
8 pass (bootstrap plus six CSS-undo rows), 0 fail, 787 not-run. Its 1,902 inputs
remained at seal
`d67cbd503980314c687554da72e7431ad9d19c91bb2961dcfc45ae58f4454be3`.
No product fix followed this pass, and the preceding failure remains open.

Exploratory timing inspection also separates module-load cost from disk arrival:
the live 88-sample slice had hub module-request p95 around 271–280 ms versus
108–111 ms on the peer. These are browser request intervals, not peer-render
certification, observer calibration or matched baseline comparisons. Do not
subtract them from the measured UI latency or claim the 300 ms target achieved.

### Continuation: full diagnostic run and cloud readback

Full frozen candidate `2026-09-22T18-03-24.145Z` is running against baseline
`2026-09-16T20-10-34.675Z` with 100 text samples per author and a 30-minute
soak. It retains the new read-only selection-order trace; the intermittent
preparation failure is not declared fixed by an isolated pass. Source/build
inputs must remain unchanged until this run terminates.

Separately, actual cloud r3 still returns revision 20/epoch 1/six documents.
Read-only authenticated GETs verify historical sources r16/r17 and both image
hashes. The designer browser renders r17's new image beside the current old
image in dark and light themes. Actual Tab enters the version picker; Escape
closes the modal and returns focus to `project-history-preview-17`. The original
dark theme was restored and no Restore action submitted. Three screenshots
were inspected. Onboarding notices overlap comparison chrome, so no clean
DS/a11y or full S19 claim is made. Detailed scope and hashes are in
`followup-2026-09-22-cloud-r3-media-readback.json`.

### Pending selection delivery across HMR; r4 deployed

The diagnostic full run `2026-09-22T18-03-24.145Z` was deliberately stopped
through its WDIO child after 591 pass, 1 fail and 3 unsupported observations.
The remaining matrix and soak did not run. The outer runner completed source
audit/teardown and returned 1; absent baseline cells are unrun, not newly
observed product failures. All 1,902 inputs and HEAD stayed unchanged; file seal
`d67cbd503980314c687554da72e7431ad9d19c91bb2961dcfc45ae58f4454be3`.
Source audit: 1,338 parsed, zero syntax failures, 900 exact copies compared,
zero missing or mismatched. All 300 inline text samples passed.

The failed native-after-peer CSS preparation records a current h1 with weight
400 before and 300 after; the only incoming message during preparation is
`loaded`, 31.5 ms before the pointer returns. No `select-set` arrives in the
following 15 seconds. This is consistent with the provider being replaced
inside the 50 ms selection debounce. Contrary to an earlier inspection
assumption, `OkSignal key={attempt}` encloses `buildCanvasTree`, including its
providers; the inner separately bundled CanvasShell provider also remounts.

An isolated source copy at `/tmp/maude-selection-race-Rc10c5/apps/studio` adds
`test/selection-hmr-delivery.test.tsx`: the real mounted HMR runtime receives
a UI selection then a replacement before the debounce can fire. Original
code delivers only `loaded` and fails. Cleanup now sends the pending selection
exactly once before that handshake. A second test proves that a newer clear
wins over two preceding selections, with no delayed duplicate.

After full-run termination, both tests failed again in the main tree before
the provider fix was applied. The affected eight-file suite passes **36 tests,
105 assertions**; studio typecheck and the two changed-file Biome checks pass.
The release-minified studio was rebuilt. Native build initially could not find
Cargo on PATH (no build ran); it was restarted with the existing
`/home/iagh/.cargo/bin` on PATH. Focused and full native results are still
required; no gesture retry or timeout change was used to conceal the failure.

Separately, cloud r4 rollout `6989e4b2-d053-4d28-bd10-3864f186aaf0` completed
on only the test cell (version 4, one active instance, zero reported errors).
The exact pushed image contains the text-lifetime fix by source hash. All
three served bundle hashes match its manifest; the complete filtered bootstrap
is identical before/after (revision 20, epoch 1, six docs with lane hashes and
effects). See `followup-2026-09-22-cloud-r4.json`. This r4 image predates the
pending-selection fix and is not whole F3/native-cloud certification.

### Actual cloud S19 audit — blockers retained

Using the existing isolated designer-B browser on cloud r4, ran axe-core 4.13.0
against the studio shell in six states: History, accepted revision-17 preview
and Sync, each in dark and light. The scanner dependency was installed only in
`/tmp/maude-a11y-check-Uh1kOY`, not in repository dependencies or native inputs.
The generic skill script assumes localhost 4000/4001 and a fresh unauthenticated
browser, so the same WCAG 2.1 AA tag set was run through the existing authenticated
browser instead. Raw reports are `.ai/browser/eval/f3-r4-a11y-*.json`; deduplicated
exact findings are in `followup-2026-09-22-cloud-a11y.json` (six scans, 38 distinct
rule/target/failure combinations across the two themes).

All scans fail: menu/tree ownership (`aria-required-children` and
`aria-required-parent`), canvas iframe name, root language, and contrast.
Unselected tabs/sign-out and status-bar labels use the low-contrast fg-3 token;
light project text also falls short. These are required S19 follow-up work, not
waived as existing UI. Automated shell results do not certify cross-origin
canvas contents, screen-reader behavior, native UI or self-host. Preview and
Sync were closed/left through actual controls; designer B is back on History,
dark theme, with no modal and no accepted content/history mutation.

Pending-selection fix was rebuilt into the Linux debug app successfully.
Focused run `2026-09-22T18-23-28.273Z` completed with **319 pass, 1 fail,
1 unsupported, 771 not-run**. All 300 text edits and six CSS undo rows pass.
Source seal `4654be7f218524e8ea43a15385942356d8645933c6a6e8788c64a73d9524d8e4`
remained unchanged across all 1,903 inputs. Source audit: 1,353 parsed, zero
syntax failures, 900 compared, zero mismatched/missing. Native history preview
fails `preview-render` before Restore: the screenshot shows the saved original
but a blank current-version pane. Root cause is not established. All eight
generated hub/peer-direction preview/restore PNGs and the native failure PNG
were inspected. `L18-preview-restored-peer-native.png` still shows the failed
native dialog, so underlying DOM readback must not be called visible peer
certification. F1/F3/F4 remain open; no full run is active.

### S19 shell semantic and contrast repairs — partial local verification

After the focused run was terminal, added `shell-accessibility.test.ts` using
the real JSX AST and shell HTML: all three tests failed before moving menubar
ownership to `.st-menus`, adding `lang="en"`, and naming canvas frames with
their paths. No ARIA role is removed from the actual menu controls. Header
retains its native landmark semantics (WAI-ARIA menu ownership reference:
https://www.w3.org/WAI/ARIA/apg/patterns/menubar/).

Added `shell-text-contrast.test.ts` using real CSS declarations, Maude theme
tokens and the existing OKLCH/WCAG implementation. Eleven combinations failed;
the already-passing dark project label is retained as a control. Readable text
now uses fg-2 for project name, status labels/version, inactive dock tabs,
preview zoom-lock label and sign-out. All 12 theme/selector combinations now
clear 4.5:1 on bg-1 and bg-2. Existing token definitions are unchanged.

The combined affected suite passes **21 tests, 60 assertions, five files**;
studio typecheck and Biome on both new test files pass. Release-minified client
and styles were rebuilt without runtime regeneration. Local Chromium audits
exercise the actual rebuilt shell on an isolated HTML-canvas fixture, using
the actual theme button and reading `.maude[data-theme]` (changing only the
root HTML theme would not switch the shell). Both settled scans retain only
the known tree-ownership violation. An earlier loading-time dark scan also
reported `.st-skel-cap` contrast: preserved in evidence, not waived by waiting
for the settled state. Both final theme screenshots were inspected.

See `followup-2026-09-22-shell-a11y-fixes.json` for source/build hashes, scan
outputs and limits. This fixture does not render cloud sign-out or accepted
preview, so their CSS tests are not actual-cloud rendered certification. Cloud
r4 and the previous native artifact predate these fixes. Tree semantics,
loading state, real backend/native verification and manual focus/screen-reader
checks remain open. No plan archive, commit or push has happened.

### S19 tree ownership and keyboard repair — local verification

`FileTree` / `FileTreeItem` now provide the real tree hierarchy: a named
treeitem owns its primary native button, independent row actions and a child
group. Section, directory, design-system, file and canvas rows all use it.
Roving focus supports Up/Down, Left/Right, Home/End, typeahead and native
activation; action menus remain separately keyboard-accessible. Escape returns
from a row action to its treeitem. The tour still targets the actual canvas row.

The real React navigation test and source ownership guard failed before wiring.
Both loading-caption contrast tests failed before changing fg-3 to fg-2.
An additional real React reproduction found focus stranded on the empty root
after asynchronous population: that case failed while the outside-focus control
passed. Tab-stop repair now focuses the loaded row only if the root itself owns
focus. Combined verification: **25 pass / 0 fail / 75 assertions / four files**.
Studio typecheck and Biome on all three new test files pass. Catalogue and
serialized drag payload checks pass (two tests); those are not UI drag proof.

Actual local Chromium checks the complex expanded tree with nested canvas,
sidecar CSS and design-system children. Thirty keyboard checks pass in the two
themes, including action-menu dismissal/focus restoration. A repeat after reload
initially sent the next arrow before expansion settled (11 pass, then sidecar
child failed); the driver now waits for the actual aria-expanded state. No
gesture, expected child or product timeout was relaxed. A subsequent full
30-check repeat passed. Updated axe scans of the actual rebuilt shell report
zero WCAG 2/2.1 A/AA violations in both themes. Both saved theme screenshots
were inspected. Detailed results and SHA-256 inputs are in
`followup-2026-09-22-tree-a11y.json`.

The release bundle and staged Linux native debug build both succeed. Native
focused L01/L07/L18 run `2026-09-22T19-04-45.975Z` completed **47 pass, 0 fail,
1 unsupported, 1,044 not-run**. All 1,907 source inputs retained seal
`4e2c2f8e16426456c7a291dc46b70b24687cd8c2e59f5318ab53759f1e1917da`.
Source audit parsed 162 snapshots without syntax failures; text comparisons
were not exercised in this selection. All three preview restores and all six
CSS undo cases pass. All twelve preview/restore screenshots were inspected;
this is not an inspection of every other screenshot or full visual certification.
The native narrow window still shows clipped shell chrome and a tour toast
above the dialog: no blanket S19 visual approval is inferred. Preview failures
now capture their last heading probes, frame paths,
bounds and resource timings without exposing signed query parameters. The last
native blank-current-preview failure remains unresolved, not hidden by reruns.
Real-cloud/native/self-host S19, manual screen-reader/dialog coverage, the full
F1 surface run and F4 target still require verification. Cloud r4 is unchanged.

Full candidate `2026-09-22T19-09-46.637Z` is active (process session 38393):
100 text samples per author, 1,800,000 ms soak, accepted mode, comparison with
preserved baseline `2026-09-16T20-10-34.675Z`. The handle was polled live after
bootstrap; no completion is claimed. Do not edit its source/build inputs while
it runs. Local S19 fixture server/session 65678 and browser `maude-s19-local`
are retained for further accessibility checks. The tree decision was captured
once in the local-only graph as `d_ff62c26facde8f39bd9f80c7`; no remote sync.

### Actual cloud recovery loss and isolated repaired candidate

`followup-2026-09-22-cloud-recovery.json` records the actual-cloud failure:
client-invalid U1 is initially backed up; after SIGKILL, U2 repair and designer-B
color r22, restart safely holds U2 but mutable recovery slots lose original U1.
The production-code regression is red. Original fixture remains unresolved at
local port 19481; no resolution was silently selected there.

`followup-2026-09-22-cloud-recovery-candidate.json` records the isolated repair
at `/tmp/maude-recovery-fix-uRkuj6`. A bounded atomic episode record pins the
first original/base across repairs, marks successful resolution without deleting
its bytes, and allows a subsequent conflict episode. Dialog review exposes both
and reuses the existing focus helper. Four lifetime cases and two dialog cases
failed before their respective fixes. A real storage fault control also ensures
an accepted resolution cannot become a rejected promise over local bookkeeping.

Actual cloud `ui-f3repairfixed` starts r23. Invalid U1 is held, exact process
1284780 is SIGKILLed, U2 is repaired while stopped, designer B adds color as r24.
Restart retains original U1/base. Actual browser review exposes both exact
sources; Shift+Tab/Tab wrap; keyboard selection of the project version preserves
U1 and U2 backups and renders red r24 locally and in the independent owner.
First Escape observation returned focus to the canvas; a settled repeat returned
to Resolve. Keep that timing limitation open. Both expanded-dialog axe scans
(4.13.0, WCAG 2/2.1 A/AA) report zero violations; not a whole-product S19 claim.

Rendered verification found a further real defect: resolution removed the
notice but retained the source fact consumed by the summary. Two fail-before
tests cover facts with/without notice, preserving other sources and historical
notes. After fixing `clearSourceConflict`, a fresh actual U3 conflict is created
and resolved through keyboard UI: `_sync.json` has no conflicts, no stale
attention message/Resolve control remains, and the summary reports synced.
All six candidate screenshots were inspected; initial light capture was during
animation and a settled capture was also inspected. Expanded details require
scrolling at 633px height; collapsed actions/keyboard-focused action are visible.

Final scoped candidate run: **156 pass, 0 fail, 538 assertions, 10 files**,
including real hub runtimes. Studio typecheck, seven scoped Biome files and a
final release build pass. Package-manager invocation initially tried a missing
isolated lockfile install and aborted before removing dependencies; verification
used the existing explicit Biome binary instead. No dependency change made.
Candidate patch backup `.ai/browser/eval/recovery-candidate.patch` has SHA-256
`95e4eb1c025c59276e42252f791c955f3d6b07bbdb532b2d2d8e1b7f9d2a68bf`;
`git apply --check` passes, but it has NOT been applied. Source-copy server
session 11484/port 19482 and browser `maude-f3-recovery-candidate` remain for
continuation. Decision captured locally as `d_6374e5fe36f8d657b2ce5dc3`.

Full run `2026-09-22T19-09-46.637Z` remains live on session 38393. Latest observed
functional tally: **1,067 pass, 5 fail, 13 unsupported, 4 not-run**; not a final
result. Native timeline split/delete/undo/redo fail; native accepted-history
preview again has a blank current-version pane. Split/delete screenshots for
all three participants and the preview failure screenshot were inspected.
Preview diagnostics show saved heading, null current-preview heading, valid
nonzero bounds for both frames and the live canvas heading present. Timeline
split screenshot shows playhead readout zero and three clips; cause is not yet
established. Undo/redo failure screenshots still need inspection. The run
continues for the soak/final audit; do not interrupt/restart or edit main inputs
merely because observation takes time. All 1,907 input hashes were rechecked
and still match seal `4e2c2f8e16426456c7a291dc46b70b24687cd8c2e59f5318ab53759f1e1917da`.
The initial read-only manifest comparison used the wrong field (`sha256`);
corrected comparison uses `sourceManifestSha256` and asserts it is a string.

F1/F3/F4 remain open. Explicit network-dependent U1/U2 replay, native/backend
matrix, storage-failure API semantics, manual screen-reader coverage and cleanup
are not certified by this subset. No archive, repository commit/push or
production rollout occurred.
