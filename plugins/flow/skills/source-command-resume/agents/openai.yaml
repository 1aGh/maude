interface:
  display_name: "flow:resume"
  short_description: "Resume a paused workflow from its handoff and saved state."
  default_prompt: "Use $flow:source-command-resume with the task details I provide."
policy:
  allow_implicit_invocation: false
