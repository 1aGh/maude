interface:
  display_name: "flow:setup-prd"
  short_description: "Create a product requirements document with phase plans and…"
  default_prompt: "Use $flow:source-command-setup-prd with the task details I provide."
policy:
  allow_implicit_invocation: false
