interface:
  display_name: "flow:init"
  short_description: "Scaffold the .ai workspace, detect the project stack and…"
  default_prompt: "Use $flow:source-command-init with the task details I provide."
policy:
  allow_implicit_invocation: false
