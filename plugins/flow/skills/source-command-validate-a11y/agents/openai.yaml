interface:
  display_name: "flow:validate-a11y"
  short_description: "Audit project UI accessibility and report evidence."
  default_prompt: "Use $flow:source-command-validate-a11y with the task details I provide."
policy:
  allow_implicit_invocation: false
