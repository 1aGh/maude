interface:
  display_name: "flow:bug-rca"
  short_description: "Investigate a ticket and document its root cause with evidence."
  default_prompt: "Use $flow:source-command-bug-rca with the task details I provide."
policy:
  allow_implicit_invocation: false
