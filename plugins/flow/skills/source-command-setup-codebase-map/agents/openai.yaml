interface:
  display_name: "flow:setup-codebase-map"
  short_description: "Create a persistent project architecture map for later sessions."
  default_prompt: "Use $flow:source-command-setup-codebase-map with the task details I provide."
policy:
  allow_implicit_invocation: false
