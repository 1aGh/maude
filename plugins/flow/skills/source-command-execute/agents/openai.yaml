interface:
  display_name: "flow:execute"
  short_description: "Execute an implementation plan and maintain verification and…"
  default_prompt: "Use $flow:source-command-execute with the task details I provide."
policy:
  allow_implicit_invocation: false
