interface:
  display_name: "flow:maintain-clean"
  short_description: "Clean stale workflow artifacts, logs and temporary files."
  default_prompt: "Use $flow:source-command-maintain-clean with the task details I provide."
policy:
  allow_implicit_invocation: false
