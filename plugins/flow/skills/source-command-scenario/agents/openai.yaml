interface:
  display_name: "flow:scenario"
  short_description: "Run cross-platform web and native UI scenarios with…"
  default_prompt: "Use $flow:source-command-scenario with the task details I provide."
policy:
  allow_implicit_invocation: false
