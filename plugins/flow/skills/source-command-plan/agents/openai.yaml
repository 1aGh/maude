interface:
  display_name: "flow:plan"
  short_description: "Create a feature implementation plan grounded in project…"
  default_prompt: "Use $flow:source-command-plan with the task details I provide."
policy:
  allow_implicit_invocation: false
