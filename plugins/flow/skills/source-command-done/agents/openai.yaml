interface:
  display_name: "flow:done"
  short_description: "Validate and close a feature: decision capture, delivery,…"
  default_prompt: "Use $flow:source-command-done with the task details I provide."
policy:
  allow_implicit_invocation: false
