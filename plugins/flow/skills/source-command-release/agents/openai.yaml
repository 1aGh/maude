interface:
  display_name: "flow:release"
  short_description: "Follow the project release runbook and its action approval…"
  default_prompt: "Use $flow:source-command-release with the task details I provide."
policy:
  allow_implicit_invocation: false
