interface:
  display_name: "flow:validate-security"
  short_description: "Run security and adversarial reviews, consolidate findings…"
  default_prompt: "Use $flow:source-command-validate-security with the task details I provide."
policy:
  allow_implicit_invocation: false
