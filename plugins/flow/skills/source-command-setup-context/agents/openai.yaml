interface:
  display_name: "flow:setup-context"
  short_description: "Load relevant codebase context for the current project and task."
  default_prompt: "Use $flow:source-command-setup-context with the task details I provide."
policy:
  allow_implicit_invocation: false
