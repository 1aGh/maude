interface:
  display_name: "flow:record-ddr"
  short_description: "Record an architectural or product decision with its rationale."
  default_prompt: "Use $flow:source-command-record-ddr with the task details I provide."
policy:
  allow_implicit_invocation: false
