interface:
  display_name: "flow:maintain-ai-health"
  short_description: "Check project skills, agents, commands, workflow state and…"
  default_prompt: "Use $flow:source-command-maintain-ai-health with the task details I provide."
policy:
  allow_implicit_invocation: false
