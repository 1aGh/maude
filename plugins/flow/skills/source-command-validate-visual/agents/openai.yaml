interface:
  display_name: "flow:validate-visual"
  short_description: "Check visual regressions with screenshots."
  default_prompt: "Use $flow:source-command-validate-visual with the task details I provide."
policy:
  allow_implicit_invocation: false
