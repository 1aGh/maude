interface:
  display_name: "flow:pause"
  short_description: "Save workflow state and a handoff for a later session."
  default_prompt: "Use $flow:source-command-pause with the task details I provide."
policy:
  allow_implicit_invocation: false
