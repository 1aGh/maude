interface:
  display_name: "flow:maintain-docs"
  short_description: "Check documentation freshness and stale references."
  default_prompt: "Use $flow:source-command-maintain-docs with the task details I provide."
policy:
  allow_implicit_invocation: false
