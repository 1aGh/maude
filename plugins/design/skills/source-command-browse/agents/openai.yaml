interface:
  display_name: "design:browse"
  short_description: "Open the local Maude canvas browser."
  default_prompt: "Use $design:source-command-browse with the task details I provide."
policy:
  allow_implicit_invocation: false
