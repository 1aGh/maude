interface:
  display_name: "design:export"
  short_description: "Export a canvas to images, documents, slides, video or a…"
  default_prompt: "Use $design:source-command-export with the task details I provide."
policy:
  allow_implicit_invocation: false
