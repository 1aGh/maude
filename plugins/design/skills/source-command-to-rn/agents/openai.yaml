interface:
  display_name: "design:to-rn"
  short_description: "Create a React Native SVG animation fallback for light motion."
  default_prompt: "Use $design:source-command-to-rn with the task details I provide."
policy:
  allow_implicit_invocation: false
