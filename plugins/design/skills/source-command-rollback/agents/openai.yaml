interface:
  display_name: "design:rollback"
  short_description: "Restore one or more snapshots of the active canvas."
  default_prompt: "Use $design:source-command-rollback with the task details I provide."
policy:
  allow_implicit_invocation: false
