interface:
  display_name: "design:setup-ds"
  short_description: "Create or re-bootstrap a named design system."
  default_prompt: "Use $design:source-command-setup-ds with the task details I provide."
policy:
  allow_implicit_invocation: false
