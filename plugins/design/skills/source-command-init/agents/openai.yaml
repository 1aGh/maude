interface:
  display_name: "design:init"
  short_description: "Initialize project design configuration and check dependencies."
  default_prompt: "Use $design:source-command-init with the task details I provide."
policy:
  allow_implicit_invocation: false
