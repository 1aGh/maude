interface:
  display_name: "design:draw"
  short_description: "Create and verify SVG logos, icons, illustrations or…"
  default_prompt: "Use $design:source-command-draw with the task details I provide."
policy:
  allow_implicit_invocation: false
