interface:
  display_name: "design:photo"
  short_description: "Adjust or retouch a canvas photo, apply masks, or remove its…"
  default_prompt: "Use $design:source-command-photo with the task details I provide."
policy:
  allow_implicit_invocation: false
