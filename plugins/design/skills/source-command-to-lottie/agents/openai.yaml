interface:
  display_name: "design:to-lottie"
  short_description: "Export animation keyframes to a verified Lottie artifact for…"
  default_prompt: "Use $design:source-command-to-lottie with the task details I provide."
policy:
  allow_implicit_invocation: false
