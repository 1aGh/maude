interface:
  display_name: "design:generate"
  short_description: "Generate media through Maude BYOK providers and place the…"
  default_prompt: "Use $design:source-command-generate with the task details I provide."
policy:
  allow_implicit_invocation: false
