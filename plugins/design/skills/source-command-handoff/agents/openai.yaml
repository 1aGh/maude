interface:
  display_name: "design:handoff"
  short_description: "Create a shadcn registry handoff for the active canvas."
  default_prompt: "Use $design:source-command-handoff with the task details I provide."
policy:
  allow_implicit_invocation: false
