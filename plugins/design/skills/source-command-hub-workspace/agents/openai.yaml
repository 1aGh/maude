interface:
  display_name: "design:hub-workspace"
  short_description: "Set up and verify a self-hosted Maude workspace."
  default_prompt: "Use $design:source-command-hub-workspace with the task details I provide."
policy:
  allow_implicit_invocation: false
