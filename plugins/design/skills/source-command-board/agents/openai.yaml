interface:
  display_name: "design:board"
  short_description: "Read a whiteboard or create a structured board template with…"
  default_prompt: "Use $design:source-command-board with the task details I provide."
policy:
  allow_implicit_invocation: false
