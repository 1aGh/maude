interface:
  display_name: "design:edit"
  short_description: "Apply feedback to the active canvas in place, then verify…"
  default_prompt: "Use $design:source-command-edit with the task details I provide."
policy:
  allow_implicit_invocation: false
