interface:
  display_name: "design:import"
  short_description: "Import a Figma document or reconstruct a design from an image."
  default_prompt: "Use $design:source-command-import with the task details I provide."
policy:
  allow_implicit_invocation: false
