---
title: Maude, how it works mostly
description: The design tool built around Claude Code, not bolted onto one. Two plugins (design + flow), one CLI, an optional self-hosted hub. No telemetry, no signup.
---

<Callout type="info" title="Claude Code and terminal Codex">
The same canvas and workflow procedures are available through
[native Codex plugins](/docs/codex). Codex can drive Studio through the Maude CLI,
project files and browser tools. This does not switch Studio's built-in ACP chat.
</Callout>


Most design tools bolt AI onto a human workflow. Claude's own canvas is a chat box that forgets everything by morning. Maude is built around Claude Code from day one, so canvases are real files, and the same repo-native model runs `flow`'s plan-to-ship loop too. That's the whole pitch.

Two plugins, one CLI, and the `.ai/` workspace they both pretend not to need. You're probably here for `design` or `flow`. `maude` is the plumbing. Cmd-K skips ahead.

- **`design`**. Canvas-first iteration on `.tsx` canvases under `.design/`. A Bun dev server tracks the active tab + your selection over WebSocket, so Cmd+Click any element and Claude knows which element the feedback is about. An optional self-hostable hub mirrors `.design/` across collaborators.
- **`flow`**. The agentic loop that ships things eventually. Second-brain `.ai/` workspace, project-agnostic via per-repo `workflows.config.json`. Same plugin runs in Next.js, Expo, Go, anything.
- **`maude`** (legacy alias: `mdcc`). Small CLI. Scaffolds the `.ai/` workspace, reads and writes config, boots the design dev server, runs health diagnostics (`doctor`), manages the sidecar cache, and hosts the optional sync hub.

Open-source. MIT. No telemetry, no signup, no _book a demo_ button. If it crashes, `git pull` and try again. That's the entire support contract.

Prefer not to touch a terminal? Grab the native desktop app — canvas browser, git-without-git, GitHub sign-in, and live collaboration, no CLI required.

<DownloadNative />

<ArchitectureMap caption="maude at a glance — Claude Code, the two plugins, the CLI, and the .ai/ + .design/ workspaces" />

## Pick a direction

<Cards>
  <Card
    title="Getting Started"
    href="/docs/getting-started"
    description="Four steps and you're in. (Maybe five. Depends on your shell.)"
  />
  <Card
    title="`maude` CLI"
    href="/docs/cli"
    description="Subcommands: init, config, doctor, cache, design, hub."
  />
  <Card
    title="Design canvas loop"
    href="/docs/design"
    description="`/design:new`, `/design:edit`, `/design:critic`, and the rest of the canvas loop."
  />
  <Card
    title="Flow lifecycle"
    href="/docs/flow"
    description="The 30 slash commands behind plan, execute, done, validate, release."
  />
  <Card
    title="Config reference"
    href="/docs/config"
    description="Every key in `.ai/workflows.config.json` with examples."
  />
  <Card
    title="Recipes"
    href="/docs/recipes/nextjs"
    description="Drop-in setups for Next.js, Expo, and pnpm monorepos."
  />
  <Card
    title="Self-host the hub"
    href="/docs/hub"
    description="Optional Yjs sync hub — mirror `.design/` across collaborators. You run it; there's no SaaS tier."
  />
</Cards>

## Why both plugins together?

The two plugins share a worldview. **Claude Code is the IDE. The repo is the source of truth.** Decisions get written to `.ai/decisions/`. Plans get archived to `.ai/plans/archive/`. Design mocks live in `.design/` next to the production code they precede. Nothing lives only in a chat scroll.

Install one, install both. They're independent.

<StatPanel caption="The catalog at a glance" />

## License and source

MIT. Source at [github.com/1aGh/maude](https://github.com/1aGh/maude). Open issues if it breaks. Send PRs if you have a better idea. I read both.
